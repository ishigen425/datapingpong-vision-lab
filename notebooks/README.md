# notebooks

Reference notebooks imported from the previous Google Drive workspace.

Use these to understand the original experiments, then move reusable logic into `src/` and runnable entrypoints into `tasks/`.
